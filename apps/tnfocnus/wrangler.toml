name = "tnfocnus"
main = "index.js"
compatibility_date = "2024-03-09"

# 시크릿은 wrangler secret put FIREBASE_API_KEY 로 설정
# (Firebase 웹 API 키 자체는 비밀값은 아니지만, 환경별 설정 관리를 위해 env로 분리)

[[rules]]
type = "Text"
globs = ["**/*.html"]
fallthrough = true
